ktor:
  application:
    modules:
      - net.kigawa.keruta.ktse.KerutaTaskServer.module
  deployment:
    port: 8080
  security:
    defaultIdp:
      - issuer: https://user.kigawa.net/realms/develop
        audience: keruta
    defaultProvider:
      - issuer: http://127.0.0.1:5173
        audience: keruta
        name: web
zk:
  host: localhost:2181,localhost:2182,localhost:2183
db:
  jdbcUrl: ${DB_JDBC_URL}
  username: ${DB_USERNAME}
  password: ${DB_PASSWORD}
